<div class="follow-form user-<%= object.id %>" style="width: 100%; margin-top: 10px;">
  <% if current_user.following?(object) %>
    <%= render 'unfollow', object: object %>
  <% else %>
    <%= render 'follow', object: object %>
  <% end %>
</div>