$(".comments").html("<%= j(render 'posts/comment', { comments: @post.comments }) %>")
$("#comments_<%= @post.id %>").html('<%= @post.comments.count.to_s(:delimited) %>');
$("textarea").val('')
$('#comment_btn').hide();